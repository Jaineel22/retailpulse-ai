# RetailPulse AI ML service — Phase 3
# Core service
fastapi>=0.115,<0.116
uvicorn[standard]>=0.30,<0.31
pydantic>=2.7,<3.0
python-dotenv>=1.0,<2.0

# Data science / ML
pandas>=2.2,<3.0
numpy>=1.26,<3.0
scikit-learn>=1.5,<1.6

# MongoDB access (the ML service reads inventory_events directly — see README)
pymongo>=4.7,<5.0

# Testing (pytest is required by the Phase 3 blueprint; httpx is needed for
# FastAPI's TestClient)
pytest>=8.2,<9.0
httpx>=0.27,<0.28
